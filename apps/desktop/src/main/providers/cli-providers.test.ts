import { describe, expect, it } from 'vitest';
import { CLI_CONFIGS } from './cli-providers.js';
describe('CLI configs', () => { it('uses each CLI non-interactive streaming protocol', () => { expect(CLI_CONFIGS.find((item) => item.id === 'claude')?.promptArgs('x')).toEqual(['-p', '--output-format', 'stream-json', '--include-partial-messages', '--verbose', 'x']); expect(CLI_CONFIGS.find((item) => item.id === 'cursor')?.promptArgs('x')).toContain('--stream-partial-output'); expect(CLI_CONFIGS.find((item) => item.id === 'codex')?.promptArgs('x')).toEqual(['exec', '--json', 'x']); expect(CLI_CONFIGS.find((item) => item.id === 'opencode')?.promptArgs('x')).toEqual(['run', '--format', 'json', '--model', 'sensenova/sensenova-6.8-flash-lite', 'x']); }); it('wraps PowerShell entrypoints explicitly on Windows', () => { const cursor = CLI_CONFIGS.find((item) => item.id === 'cursor'); if (process.platform === 'win32') { expect(cursor?.command).toBe('powershell.exe'); expect(cursor?.commandArgs?.[0]).toBe('-NoProfile'); expect(cursor?.commandArgs).toContain('-File'); } }); });
describe('CLI configs', () => { it('uses each CLI non-interactive streaming protocol', () => { expect(CLI_CONFIGS.find((item) => item.id === 'claude')?.promptArgs('x')).toEqual(['-p', '--output-format', 'stream-json', '--include-partial-messages', '--verbose', 'x']); expect(CLI_CONFIGS.find((item) => item.id === 'cursor')?.promptArgs('x')).toContain('--stream-partial-output'); expect(CLI_CONFIGS.find((item) => item.id === 'codex')?.promptArgs('x')).toEqual(['exec', '--json', 'x']); expect(CLI_CONFIGS.find((item) => item.id === 'opencode')?.promptArgs('x')).toEqual(['run', '--format', 'json', '--model', 'sensenova/sensenova-6.8-flash-lite', 'x']); }); it('wraps PowerShell entrypoints explicitly on Windows', () => { const cursor = CLI_CONFIGS.find((item) => item.id === 'cursor'); if (process.platform === 'win32') { expect(cursor?.command).toBe('powershell.exe'); expect(cursor?.commandArgs?.[0]).toBe('-NoProfile'); expect(cursor?.commandArgs).toContain('-File'); } }); it('resolves Windows npm shims to executable entries', () => { if (process.platform === 'win32' && !process.env.CODEAGENT_CODEX_COMMAND) expect(CLI_CONFIGS.find((item) => item.id === 'codex')?.command.toLowerCase()).toMatch(/codex\.(cmd|exe)$/); }); });
