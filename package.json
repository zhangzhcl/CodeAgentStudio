{
  "name": "codeagent-studio",
  "private": true,
  "version": "0.1.0",
  "description": "CodeAgent开发平台",
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace @codeagent-studio/protocol",
    "typecheck": "npm run typecheck --workspace @codeagent-studio/protocol"
  }
}
